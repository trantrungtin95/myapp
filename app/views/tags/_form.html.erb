<%= form_with(model: tag, local: true) do |form| %>
  <div class="form-group">
    <%= form.label :attribute_name %><br>
    <%= form.text_field :attribute_name %>
  </div>
  <div class="actions">
    <%= form.submit 'Create', class: 'btn btn-primary' %>
  </div>
<% end %>