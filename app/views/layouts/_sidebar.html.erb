<h3 class="my-4">Hello <%= User.find(session[:user_id]).name if session[:user_id] %></h3>
        <div class="list-group">
          <%= link_to 'Products', products_path, class: 'list-group-item' %>
        </div>